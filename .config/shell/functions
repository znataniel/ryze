### Helper functions for shell.

add_to_path() {
  [[ -d "$1" ]] && ! echo $PATH | grep -Eq "(^|:)$1(:|$)" && export PATH="$PATH:$1"
}

get_filename_wo_fext (){
  echo $(sed "s/\(^.*\)\..*/\1/g" <<< $1)
}
